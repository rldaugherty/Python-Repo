#You can change numbers throughout the python script even if you define them at the top of the file.
quilt_width = 8
quilt_length = 12
print(quilt_width*quilt_length)

#Let's change the length to a bit smaller
quilt_length = 8
print(quilt_width*quilt_length)